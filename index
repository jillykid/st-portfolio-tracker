<!DOCTYPE html>
<html lang="sv">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ST Portfolio - Delmål & Aktiviteter</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            line-height: 1.6;
            color: #333;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
        }
        
        .container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 20px;
        }
        
        header {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            padding: 30px;
            margin-bottom: 30px;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
        }
        
        h1 {
            color: #4a5568;
            font-size: 2.5rem;
            margin-bottom: 10px;
        }
        
        .subtitle {
            color: #718096;
            font-size: 1.1rem;
        }
        
        .tabs {
            display: flex;
            background: rgba(255, 255, 255, 0.9);
            border-radius: 15px;
            padding: 8px;
            margin-bottom: 30px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
        }
        
        .tab {
            flex: 1;
            padding: 15px 25px;
            text-align: center;
            border-radius: 10px;
            cursor: pointer;
            font-weight: 500;
            transition: all 0.3s ease;
            color: #718096;
        }
        
        .tab.active {
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: white;
            box-shadow: 0 4px 15px rgba(102, 126, 234, 0.4);
        }
        
        .tab-content {
            display: none;
        }
        
        .tab-content.active {
            display: block;
        }
        
        .dashboard {
            display: grid;
            grid-template-columns: 1fr 400px;
            gap: 25px;
        }
        
        .main-content {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            padding: 30px;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
            min-height: 600px;
        }
        
        .sidebar {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            padding: 25px;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
            height: fit-content;
        }
        
        .delmal-item {
            background: #f7fafc;
            border-radius: 12px;
            padding: 20px;
            margin-bottom: 20px;
            border-left: 5px solid #e2e8f0;
            transition: all 0.3s ease;
            cursor: pointer;
        }
        
        .delmal-item:hover {
            background: #edf2f7;
            border-left-color: #667eea;
            transform: translateX(5px);
        }
        
        .delmal-item.selected {
            border-left-color: #667eea;
            background: #ebf4ff;
        }
        
        .delmal-item.completed {
            border-left-color: #48bb78;
            background: #f0fff4;
        }
        
        .delmal-title {
            font-weight: 600;
            color: #2d3748;
            margin-bottom: 8px;
            font-size: 1.1rem;
        }
        
        .delmal-code {
            font-family: 'Monaco', 'Menlo', monospace;
            font-size: 0.9rem;
            color: #667eea;
            background: rgba(102, 126, 234, 0.1);
            padding: 4px 8px;
            border-radius: 6px;
            display: inline-block;
            margin-bottom: 10px;
        }
        
        .delmal-progress {
            display: flex;
            align-items: center;
            gap: 10px;
            margin-top: 10px;
        }
        
        .progress-bar {
            flex: 1;
            height: 8px;
            background: #e2e8f0;
            border-radius: 4px;
            overflow: hidden;
        }
        
        .progress-fill {
            height: 100%;
            background: linear-gradient(90deg, #667eea, #764ba2);
            border-radius: 4px;
            transition: width 0.3s ease;
        }
        
        .progress-text {
            font-size: 0.9rem;
            color: #718096;
            font-weight: 500;
        }
        
        .activity-list {
            margin-top: 20px;
        }
        
        .activity-item {
            background: #f8fafc;
            border-radius: 10px;
            padding: 18px;
            margin-bottom: 15px;
            border: 2px solid transparent;
            transition: all 0.3s ease;
            position: relative;
        }
        
        .activity-item:hover {
            border-color: #cbd5e0;
            background: #f1f5f9;
        }
        
        .activity-header {
            display: flex;
            justify-content: between;
            align-items: flex-start;
            margin-bottom: 12px;
        }
        
        .activity-type {
            padding: 6px 12px;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: 500;
            margin-bottom: 8px;
            display: inline-block;
        }
        
        .activity-type.kursintyg {
            background: #e6fffa;
            color: #285e61;
        }
        
        .activity-type.klinisk {
            background: #fef5e7;
            color: #744210;
        }
        
        .activity-type.sit-in {
            background: #f0fff4;
            color: #276749;
        }
        
        .activity-type.evaluation-360 {
            background: #fdf2f8;
            color: #97266d;
        }
        
        .activity-type.annat {
            background: #f7fafc;
            color: #4a5568;
        }
        
        .activity-title {
            font-weight: 600;
            color: #2d3748;
            margin-bottom: 8px;
        }
        
        .activity-meta {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 10px;
            font-size: 0.9rem;
            color: #718096;
        }
        
        .activity-date {
            display: flex;
            align-items: center;
            gap: 5px;
        }
        
        .activity-assessor {
            display: flex;
            align-items: center;
            gap: 5px;
        }
        
        .delmal-tags {
            margin-top: 12px;
            display: flex;
            flex-wrap: wrap;
            gap: 6px;
        }
        
        .delmal-tag {
            background: #667eea;
            color: white;
            padding: 4px 8px;
            border-radius: 12px;
            font-size: 0.75rem;
            font-weight: 500;
        }
        
        .add-btn {
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: white;
            border: none;
            padding: 15px 30px;
            border-radius: 25px;
            cursor: pointer;
            font-weight: 500;
            transition: all 0.3s ease;
            margin-bottom: 20px;
            display: flex;
            align-items: center;
            gap: 8px;
        }
        
        .add-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(102, 126, 234, 0.4);
        }
        
        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.6);
            z-index: 1000;
            backdrop-filter: blur(4px);
        }
        
        .modal-content {
            background: white;
            border-radius: 20px;
            padding: 35px;
            max-width: 600px;
            margin: 30px auto;
            box-shadow: 0 25px 80px rgba(0, 0, 0, 0.3);
            max-height: 90vh;
            overflow-y: auto;
        }
        
        .form-group {
            margin-bottom: 20px;
        }
        
        .form-row {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
        }
        
        label {
            display: block;
            margin-bottom: 8px;
            font-weight: 600;
            color: #4a5568;
        }
        
        input, select, textarea {
            width: 100%;
            padding: 12px 16px;
            border: 2px solid #e2e8f0;
            border-radius: 10px;
            font-size: 1rem;
            transition: all 0.3s ease;
        }
        
        input:focus, select:focus, textarea:focus {
            outline: none;
            border-color: #667eea;
            box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
        }
        
        .delmal-selector {
            border: 2px solid #e2e8f0;
            border-radius: 10px;
            padding: 15px;
            max-height: 200px;
            overflow-y: auto;
        }
        
        .delmal-checkbox {
            display: flex;
            align-items: center;
            gap: 10px;
            padding: 8px;
            margin-bottom: 8px;
            border-radius: 6px;
            transition: background 0.2s ease;
        }
        
        .delmal-checkbox:hover {
            background: #f7fafc;
        }
        
        .delmal-checkbox input {
            width: auto;
            margin: 0;
        }
        
        .form-actions {
            display: flex;
            gap: 15px;
            justify-content: flex-end;
            margin-top: 30px;
        }
        
        .btn-secondary {
            background: #e2e8f0;
            color: #4a5568;
            border: none;
            padding: 12px 24px;
            border-radius: 10px;
            cursor: pointer;
            font-weight: 500;
            transition: all 0.3s ease;
        }
        
        .btn-secondary:hover {
            background: #cbd5e0;
        }
        
        .sync-status {
            background: rgba(72, 187, 120, 0.1);
            border: 1px solid #48bb78;
            color: #276749;
            padding: 15px;
            border-radius: 12px;
            margin-bottom: 20px;
            text-align: center;
            font-weight: 500;
        }
        
        .filter-section {
            margin-bottom: 25px;
            padding: 20px;
            background: #f8fafc;
            border-radius: 12px;
        }
        
        .filter-row {
            display: flex;
            gap: 15px;
            align-items: center;
        }
        
        .filter-row select {
            flex: 1;
        }
        
        @media (max-width: 1024px) {
            .dashboard {
                grid-template-columns: 1fr;
            }
            
            .tabs {
                flex-direction: column;
            }
        }
        
        @media (max-width: 768px) {
            .container {
                padding: 15px;
            }
            
            h1 {
                font-size: 2rem;
            }
            
            .form-row {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>🏥 ST Portfolio</h1>
            <p class="subtitle">Spåra dina delmål och dokumenterade aktiviteter</p>
        </header>

        <div class="sync-status">
            ✅ Synkroniserad med Google Sheets • Senast uppdaterad: Idag 14:30
        </div>

        <div class="tabs">
            <div class="tab active" onclick="switchTab('overview')">📊 Översikt</div>
            <div class="tab" onclick="switchTab('activities')">📋 Aktiviteter</div>
            <div class="tab" onclick="switchTab('delmal')">🎯 Delmål</div>
        </div>

        <!-- Overview Tab -->
        <div id="overview" class="tab-content active">
            <div class="dashboard">
                <div class="main-content">
                    <h2 style="margin-bottom: 20px; color: #4a5568;">Senaste Aktiviteter</h2>
                    
                    <div class="activity-list">
                        <div class="activity-item">
                            <div class="activity-type kursintyg">Kursintyg</div>
                            <div class="activity-title">Psykiatri Grundkurs</div>
                            <div class="activity-meta">
                                <div class="activity-date">📅 2025-05-20</div>
                                <div class="activity-assessor">👨‍⚕️ Dr. Andersson</div>
                            </div>
                            <div class="delmal-tags">
                                <span class="delmal-tag">A1</span>
                                <span class="delmal-tag">B1</span>
                                <span class="delmal-tag">C1</span>
                            </div>
                        </div>

                        <div class="activity-item">
                            <div class="activity-type klinisk">Klinisk tjänstgöring</div>
                            <div class="activity-title">Psykiatriska kliniken - 6 månader</div>
                            <div class="activity-meta">
                                <div class="activity-date">📅 2025-06-01</div>
                                <div class="activity-assessor">👩‍⚕️ Dr. Johansson</div>
                            </div>
                            <div class="delmal-tags">
                                <span class="delmal-tag">B1</span>
                                <span class="delmal-tag">C1</span>
                                <span class="delmal-tag">C3</span>
                                <span class="delmal-tag">C4</span>
                            </div>
                        </div>

                        <div class="activity-item">
                            <div class="activity-type sit-in">Sit-in</div>
                            <div class="activity-title">Rättspsykiatrisk klinik</div>
                            <div class="activity-meta">
                                <div class="activity-date">📅 2025-03-22</div>
                                <div class="activity-assessor">👨‍⚕️ Dr. Lindqvist</div>
                            </div>
                            <div class="delmal-tags">
                                <span class="delmal-tag">C10</span>
                                <span class="delmal-tag">A6</span>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="sidebar">
                    <h3 style="margin-bottom: 20px; color: #4a5568;">Delmål Progress</h3>
                    
                    <div class="delmal-item completed">
                        <div class="delmal-code">A3</div>
                        <div class="delmal-title">Vårdhygien o smittskydd</div>
                        <div class="delmal-progress">
                            <div class="progress-bar">
                                <div class="progress-fill" style="width: 100%;"></div>
                            </div>
                            <div class="progress-text">3/3</div>
                        </div>
                    </div>

                    <div class="delmal-item">
                        <div class="delmal-code">B1</div>
                        <div class="delmal-title">Kommunikation med patienter och närstående</div>
                        <div class="delmal-progress">
                            <div class="progress-bar">
                                <div class="progress-fill" style="width: 80%;"></div>
                            </div>
                            <div class="progress-text">4/5</div>
                        </div>
                    </div>

                    <div class="delmal-item">
                        <div class="delmal-code">C1</div>
                        <div class="delmal-title">Vanliga och viktiga psykiska sjukdomar</div>
                        <div class="delmal-progress">
                            <div class="progress-bar">
                                <div class="progress-fill" style="width: 63%;"></div>
                            </div>
                            <div class="progress-text">5/8</div>
                        </div>
                    </div>

                    <div class="delmal-item">
                        <div class="delmal-code">C4</div>
                        <div class="delmal-title">Psykofarmakologisk och elektrokonvulsiv behandling</div>
                        <div class="delmal-progress">
                            <div class="progress-bar">
                                <div class="progress-fill" style="width: 67%;"></div>
                            </div>
                            <div class="progress-text">4/6</div>
                        </div>
                    </div>

                    <div class="delmal-item">
                        <div class="delmal-code">C8</div>
                        <div class="delmal-title">Utvecklingsrelaterade psykiska sjukdomar, barnpsykiatri</div>
                        <div class="delmal-progress">
                            <div class="progress-bar">
                                <div class="progress-fill" style="width: 0%;"></div>
                            </div>
                            <div class="progress-text">0/3</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Activities Tab -->
        <div id="activities" class="tab-content">
            <div class="main-content">
                <button class="add-btn" onclick="openModal('activityModal')">
                    + Lägg till aktivitet
                </button>

                <div class="filter-section">
                    <div class="filter-row">
                        <select id="typeFilter">
                            <option value="">Alla aktivitetstyper</option>
                            <option value="kursintyg">Kursintyg</option>
                            <option value="klinisk">Klinisk tjänstgöring</option>
                            <option value="sit-in">Sit-in</option>
                            <option value="evaluation-360">360° Utvärdering</option>
                            <option value="annat">Annat</option>
                        </select>
                        <select id="delmalFilter">
                            <option value="">Alla delmål</option>
                            <optgroup label="A-serien: Allmänna kompetenser">
                                <option value="A1">A1 - Medarbetarskap, ledarskap o pedagogik</option>
                                <option value="A2">A2 - Etik, mångfald o jämlikhet</option>
                                <option value="A3">A3 - Vårdhygien o smittskydd</option>
                                <option value="A4">A4 - Systematiskt kvalitets- o patientsäkerhetsarbete</option>
                                <option value="A5">A5 - Medicinsk vetenskap</option>
                                <option value="A6">A6 - Lagar och andra föreskrifter samt hälso- och sjukvårdens organisation</option>
                            </optgroup>
                            <optgroup label="B-serien: Patientvård grundläggande">
                                <option value="B1">B1 - Kommunikation med patienter och närstående</option>
                                <option value="B2">B2 - Sjukdomsförebyggande arbete</option>
                                <option value="B3">B3 - Läkemedel</option>
                                <option value="B4">B4 - Försäkringsmedicin</option>
                                <option value="B5">B5 - Palliativ vård i livets slutskede</option>
                            </optgroup>
                            <optgroup label="C-serien: Psykiatrispecifika kompetenser">
                                <option value="C1">C1 - Vanliga psykiska sjukdomar</option>
                                <option value="C2">C2 - Övriga psykiska sjukdomar</option>
                                <option value="C3">C3 - Akutpsykiatriska tillstånd</option>
                                <option value="C4">C4 - Psykofarmakologi och ECT</option>
                                <option value="C5">C5 - Somatisk differentialdiagnostik</option>
                                <option value="C6">C6 - Substansberoendetillstånd</option>
                                <option value="C7">C7 - Neurodegenerativa sjukdomar</option>
                                <option value="C8">C8 - Barnpsykiatri</option>
                                <option value="C9">C9 - Psykologiska behandlingsmetoder</option>
                                <option value="C10">C10 - Rättspsykiatri</option>
                                <option value="C11">C11 - Kognitiva funktionshinder</option>
                                <option value="C12">C12 - Samordning och uppföljning</option>
                                <option value="C13">C13 - Myndigheters regelverk</option>
                            </optgroup>
                        </select>
                        <select id="assessorFilter">
                            <option value="">Alla bedömare</option>
                            <option value="Dr. Andersson">Dr. Andersson</option>
                            <option value="Dr. Johansson">Dr. Johansson</option>
                        </select>
                    </div>
                </div>

                <div class="activity-list" id="activityList">
                    <!-- Activities will be populated here -->
                </div>
            </div>
        </div>

        <!-- Delmål Tab -->
        <div id="delmal" class="tab-content">
            <div class="main-content">
                <h2 style="margin-bottom: 20px; color: #4a5568;">Alla Delmål</h2>
                <div id="delmalList">
                    <!-- Delmål will be populated here -->
                </div>
            </div>
        </div>
    </div>

    <!-- Add Activity Modal -->
    <div id="activityModal" class="modal">
        <div class="modal-content">
            <h2 style="margin-bottom: 25px; color: #4a5568;">Lägg till ny aktivitet</h2>
            <form>
                <div class="form-group">
                    <label for="activityTitle">Aktivitetstitel</label>
                    <input type="text" id="activityTitle" placeholder="T.ex. Allmänmedicin Grundkurs">
                </div>
                
                <div class="form-row">
                    <div class="form-group">
                        <label for="activityType">Typ av aktivitet</label>
                        <select id="activityType">
                            <option value="kursintyg">Kursintyg</option>
                            <option value="klinisk">Intyg för klinisk tjänstgöring</option>
                            <option value="sit-in">Sit-in</option>
                            <option value="evaluation-360">360° Utvärdering</option>
                            <option value="annat">Annat</option>
                        </select>
                    </div>
                    
                    <div class="form-group">
                        <label for="activityDate">Datum</label>
                        <input type="date" id="activityDate">
                    </div>
                </div>
                
                <div class="form-group">
                    <label for="activityAssessor">Ansvarig bedömare</label>
                    <input type="text" id="activityAssessor" placeholder="T.ex. Dr. Andersson">
                </div>
                
                <div class="form-group">
                    <label>Relaterade delmål</label>
                    <div class="delmal-selector" id="delmalSelector">
                        <div style="font-weight: 600; color: #667eea; margin: 10px 0 8px 0; border-bottom: 1px solid #e2e8f0; padding-bottom: 4px;">A-serien: Allmänna kompetenser</div>
                        <div class="delmal-checkbox">
                            <input type="checkbox" id="delmal_a1" value="A1">
                            <label for="delmal_a1">A1 - Medarbetarskap, ledarskap o pedagogik</label>
                        </div>
                        <div class="delmal-checkbox">
                            <input type="checkbox" id="delmal_a2" value="A2">
                            <label for="delmal_a2">A2 - Etik, mångfald o jämlikhet</label>
                        </div>
                        <div class="delmal-checkbox">
                            <input type="checkbox" id="delmal_a3" value="A3">
                            <label for="delmal_a3">A3 - Vårdhygien o smittskydd</label>
                        </div>
                        <div class="delmal-checkbox">
                            <input type="checkbox" id="delmal_a4" value="A4">
                            <label for="delmal_a4">A4 - Systematiskt kvalitets- o patientsäkerhetsarbete</label>
                        </div>
                        <div class="delmal-checkbox">
                            <input type="checkbox" id="delmal_a5" value="A5">
                            <label for="delmal_a5">A5 - Medicinsk vetenskap</label>
                        </div>
                        <div class="delmal-checkbox">
                            <input type="checkbox" id="delmal_a6" value="A6">
                            <label for="delmal_a6">A6 - Lagar och andra föreskrifter samt hälso- och sjukvårdens organisation</label>
                        </div>
                        
                        <div style="font-weight: 600; color: #667eea; margin: 15px 0 8px 0; border-bottom: 1px solid #e2e8f0; padding-bottom: 4px;">B-serien: Patientvård grundläggande</div>
                        <div class="delmal-checkbox">
                            <input type="checkbox" id="delmal_b1" value="B1">
                            <label for="delmal_b1">B1 - Kommunikation med patienter och närstående</label>
                        </div>
                        <div class="delmal-checkbox">
                            <input type="checkbox" id="delmal_b2" value="B2">
                            <label for="delmal_b2">B2 - Sjukdomsförebyggande arbete</label>
                        </div>
                        <div class="delmal-checkbox">
                            <input type="checkbox" id="delmal_b3" value="B3">
                            <label for="delmal_b3">B3 - Läkemedel</label>
                        </div>
                        <div class="delmal-checkbox">
                            <input type="checkbox" id="delmal_b4" value="B4">
                            <label for="delmal_b4">B4 - Försäkringsmedicin</label>
                        </div>
                        <div class="delmal-checkbox">
                            <input type="checkbox" id="delmal_b5" value="B5">
                            <label for="delmal_b5">B5 - Palliativ vård i livets slutskede</label>
                        </div>
                        
                        <div style="font-weight: 600; color: #667eea; margin: 15px 0 8px 0; border-bottom: 1px solid #e2e8f0; padding-bottom: 4px;">C-serien: Psykiatrispecifika kompetenser</div>
                        <div class="delmal-checkbox">
                            <input type="checkbox" id="delmal_c1" value="C1">
                            <label for="delmal_c1">C1 - Vanliga och viktiga psykiska sjukdomar med betoning på de stora folksjukdomarna</label>
                        </div>
                        <div class="delmal-checkbox">
                            <input type="checkbox" id="delmal_c2" value="C2">
                            <label for="delmal_c2">C2 - Övriga psykiska sjukdomar</label>
                        </div>
                        <div class="delmal-checkbox">
                            <input type="checkbox" id="delmal_c3" value="C3">
                            <label for="delmal_c3">C3 - Akutpsykiatriska tillstånd samt katastrofpsykiatriska situationer</label>
                        </div>
                        <div class="delmal-checkbox">
                            <input type="checkbox" id="delmal_c4" value="C4">
                            <label for="delmal_c4">C4 - Psykofarmakologisk och elektrokonvulsiv behandling</label>
                        </div>
                        <div class="delmal-checkbox">
                            <input type="checkbox" id="delmal_c5" value="C5">
                            <label for="delmal_c5">C5 - Differentialdiagnostik, handlägga somatiska tillstånd (intermed, neurologiska)</label>
                        </div>
                        <div class="delmal-checkbox">
                            <input type="checkbox" id="delmal_c6" value="C6">
                            <label for="delmal_c6">C6 - Substansberoendetillstånd</label>
                        </div>
                        <div class="delmal-checkbox">
                            <input type="checkbox" id="delmal_c7" value="C7">
                            <label for="delmal_c7">C7 - Neurodegenerativa, demens sjukdomar & kognitiv störning</label>
                        </div>
                        <div class="delmal-checkbox">
                            <input type="checkbox" id="delmal_c8" value="C8">
                            <label for="delmal_c8">C8 - Utvecklingsrelaterade psykiska sjukdomar, barnpsykiatri</label>
                        </div>
                        <div class="delmal-checkbox">
                            <input type="checkbox" id="delmal_c9" value="C9">
                            <label for="delmal_c9">C9 - Teoretiska grunderna för de olika psykologiska behandlingsmetoderna</label>
                        </div>
                        <div class="delmal-checkbox">
                            <input type="checkbox" id="delmal_c10" value="C10">
                            <label for="delmal_c10">C10 - Lagar och andra föreskrifter, rättspsykiatrin</label>
                        </div>
                        <div class="delmal-checkbox">
                            <input type="checkbox" id="delmal_c11" value="C11">
                            <label for="delmal_c11">C11 - Kognitiva funktionshinder, testmetoder</label>
                        </div>
                        <div class="delmal-checkbox">
                            <input type="checkbox" id="delmal_c12" value="C12">
                            <label for="delmal_c12">C12 - Ha kunskap om samordning och uppföljning av patienter, brukarorganisationers roll</label>
                        </div>
                        <div class="delmal-checkbox">
                            <input type="checkbox" id="delmal_c13" value="C13">
                            <label for="delmal_c13">C13 - Ha kunskap relevanta myndigheters regelverk och kunna tillämpa lagar och andra föreskrifter</label>
                        </div>
                    </div>
                </div>
                
                <div class="form-group">
                    <label for="activityNotes">Anteckningar</label>
                    <textarea id="activityNotes" rows="3" placeholder="Fritext anteckningar om aktiviteten..."></textarea>
                </div>
                
                <div class="form-actions">
                    <button type="button" class="btn-secondary" onclick="closeModal('activityModal')">Avbryt</button>
                    <button type="button" class="add-btn" onclick="addActivity()">Spara aktivitet</button>
                </div>
            </form>
        </div>
    </div>

    <script>
        // Backend API configuration
        const API_BASE_URL = 'https://script.google.com/macros/s/AKfycbycu7-UYi4PXz7qg833ZgxLf2Csl3WZ5QVTpSVLcnd9nOxAF52wqvuVyM8Dmipq2LGNsA/exec';
        
        // Global data storage
        let currentActivities = [];
        let currentDelmalData = [];
        let currentDelmalProgress = [];

        // =============================================================================
        // API FUNCTIONS
        // =============================================================================
        
        async function fetchFromAPI(action) {
            try {
                const response = await fetch(`${API_BASE_URL}?action=${action}`);
                if (!response.ok) {
                    throw new Error(`HTTP error! status: ${response.status}`);
                }
                const data = await response.json();
                return data;
            } catch (error) {
                console.error('API Error:', error);
                showError('Fel vid hämtning av data från Google Sheets');
                return null;
            }
        }
        
        async function postToAPI(action, data) {
            try {
                const response = await fetch(`${API_BASE_URL}?action=${action}`, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify(data)
                });
                
                if (!response.ok) {
                    throw new Error(`HTTP error! status: ${response.status}`);
                }
                
                const result = await response.json();
                return result;
            } catch (error) {
                console.error('API Error:', error);
                showError('Fel vid sparande till Google Sheets');
                return null;
            }
        }
        
        async function loadDelmalData() {
            const data = await fetchFromAPI('getDelmal');
            if (data) {
                currentDelmalData = data;
            }
            return data;
        }
        
        async function loadActivities() {
            const data = await fetchFromAPI('getAktiviteter');
            if (data) {
                currentActivities = data;
            }
            return data;
        }
        
        async function loadDelmalProgress() {
            const data = await fetchFromAPI('getDelmalProgress');
            if (data) {
                currentDelmalProgress = data;
            }
            return data;
        }
        
        function showError(message) {
            // Create or update error message
            let errorDiv = document.getElementById('error-message');
            if (!errorDiv) {
                errorDiv = document.createElement('div');
                errorDiv.id = 'error-message';
                errorDiv.style.cssText = 'background: #fed7d7; color: #c53030; padding: 15px; border-radius: 10px; margin: 20px 0; border: 1px solid #feb2b2;';
                document.querySelector('.container').insertBefore(errorDiv, document.querySelector('header').nextSibling);
            }
            errorDiv.innerHTML = `⚠️ ${message}`;
            
            // Auto-hide after 5 seconds
            setTimeout(() => {
                if (errorDiv.parentNode) {
                    errorDiv.parentNode.removeChild(errorDiv);
                }
            }, 5000);
        }
        
        function showSuccess(message) {
            // Create or update success message
            let successDiv = document.getElementById('success-message');
            if (!successDiv) {
                successDiv = document.createElement('div');
                successDiv.id = 'success-message';
                successDiv.style.cssText = 'background: #c6f6d5; color: #276749; padding: 15px; border-radius: 10px; margin: 20px 0; border: 1px solid #9ae6b4;';
                document.querySelector('.container').insertBefore(successDiv, document.querySelector('header').nextSibling);
            }
            successDiv.innerHTML = `✅ ${message}`;
            
            // Auto-hide after 3 seconds
            setTimeout(() => {
                if (successDiv.parentNode) {
                    successDiv.parentNode.removeChild(successDiv);
                }
            }, 3000);
        }
        // =============================================================================
        // TAB SWITCHING & LOADING FUNCTIONS
        // =============================================================================
        
        async function switchTab(tabName) {
            // Hide all tab contents
            document.querySelectorAll('.tab-content').forEach(tab => {
                tab.classList.remove('active');
            });
            
            // Remove active class from all tabs
            document.querySelectorAll('.tab').forEach(tab => {
                tab.classList.remove('active');
            });
            
            // Show selected tab content
            document.getElementById(tabName).classList.add('active');
            
            // Add active class to clicked tab
            event.target.classList.add('active');
            
            // Load content for the tab
            if (tabName === 'activities') {
                await loadActivitiesTab();
            } else if (tabName === 'delmal') {
                await loadDelmalTab();
            }
        }

        // Load activities tab
        async function loadActivitiesTab() {
            const container = document.getElementById('activityList');
            container.innerHTML = '<div style="text-align: center; padding: 20px;">Laddar aktiviteter...</div>';
            
            const activities = await loadActivities();
            container.innerHTML = '';
            
            if (activities && activities.length > 0) {
                activities.forEach(activity => {
                    const activityEl = createActivityElement(activity);
                    container.appendChild(activityEl);
                });
            } else {
                container.innerHTML = '<div style="text-align: center; padding: 40px; color: #718096;">Inga aktiviteter finns ännu. Lägg till din första aktivitet!</div>';
            }
        }

        // Load delmål tab
        async function loadDelmalTab() {
            const container = document.getElementById('delmalList');
            container.innerHTML = '<div style="text-align: center; padding: 20px;">Laddar delmål...</div>';
            
            const delmalProgress = await loadDelmalProgress();
            container.innerHTML = '';
            
            if (delmalProgress && delmalProgress.length > 0) {
                delmalProgress.forEach(delmal => {
                    const delmalEl = createDelmalElement(delmal);
                    container.appendChild(delmalEl);
                });
            } else {
                container.innerHTML = '<div style="text-align: center; padding: 40px; color: #718096;">Kunde inte ladda delmål data.</div>';
            }
        }

        // Load overview sidebar with real data
        async function loadOverviewSidebar() {
            const container = document.querySelector('.sidebar');
            if (!container) return;
            
            // Keep the header
            const header = container.querySelector('h3');
            container.innerHTML = '<div style="text-align: center; padding: 20px;">Laddar progress...</div>';
            
            const delmalProgress = await loadDelmalProgress();
            container.innerHTML = '';
            
            if (header) {
                container.appendChild(header);
            }
            
            if (delmalProgress && delmalProgress.length > 0) {
                // Show top 5 delmål with most progress or that are completed
                const topDelmal = delmalProgress
                    .sort((a, b) => {
                        // Sort by completion status first, then by progress
                        const aCompleted = a.completed >= a.required_count;
                        const bCompleted = b.completed >= b.required_count;
                        if (aCompleted && !bCompleted) return -1;
                        if (!aCompleted && bCompleted) return 1;
                        return b.progress - a.progress;
                    })
                    .slice(0, 5);
                
                topDelmal.forEach(delmal => {
                    const delmalEl = createDelmalElement(delmal);
                    container.appendChild(delmalEl);
                });
            }
        }

        // Load recent activities for overview
        async function loadRecentActivities() {
            const container = document.querySelector('#overview .activity-list');
            if (!container) return;
            
            container.innerHTML = '<div style="text-align: center; padding: 20px;">Laddar senaste aktiviteter...</div>';
            
            const activities = await loadActivities();
            container.innerHTML = '';
            
            if (activities && activities.length > 0) {
                // Show most recent 3 activities
                const recentActivities = activities
                    .sort((a, b) => new Date(b.date) - new Date(a.date))
                    .slice(0, 3);
                
                recentActivities.forEach(activity => {
                    const activityEl = createActivityElement(activity);
                    container.appendChild(activityEl);
                });
            } else {
                container.innerHTML = '<div style="text-align: center; padding: 40px; color: #718096;">Inga aktiviteter finns ännu.</div>';
            }
        }

        // Create activity element
        function createActivityElement(activity) {
            const div = document.createElement('div');
            div.className = 'activity-item';
            
            const typeNames = {
                'kursintyg': 'Kursintyg',
                'klinisk': 'Klinisk tjänstgöring',
                'sit-in': 'Sit-in',
                'evaluation-360': '360° Utvärdering',
                'annat': 'Annat'
            };
            
            div.innerHTML = `
                <div class="activity-type ${activity.type}">${typeNames[activity.type]}</div>
                <div class="activity-title">${activity.title}</div>
                <div class="activity-meta">
                    <div class="activity-date">📅 ${activity.date}</div>
                    <div class="activity-assessor">👨‍⚕️ ${activity.assessor}</div>
                </div>
                <div class="delmal-tags">
                    ${activity.delmal.map(code => `<span class="delmal-tag">${code}</span>`).join('')}
                </div>
            `;
            
            return div;
        }

        // Create delmål element using real progress data
        function createDelmalElement(delmal) {
            const div = document.createElement('div');
            const progress = delmal.progress || Math.round((delmal.completed / delmal.required_count) * 100);
            const isCompleted = delmal.completed >= delmal.required_count;
            
            div.className = `delmal-item ${isCompleted ? 'completed' : ''}`;
            
            div.innerHTML = `
                <div class="delmal-code">${delmal.code}</div>
                <div class="delmal-title">${delmal.title}</div>
                <div class="delmal-progress">
                    <div class="progress-bar">
                        <div class="progress-fill" style="width: ${Math.min(progress, 100)}%;"></div>
                    </div>
                    <div class="progress-text">${delmal.completed}/${delmal.required_count}</div>
                </div>
            `;
            
            return div;
        }

        // Modal functions with real data loading
        async function openModal(modalId) {
            if (modalId === 'activityModal') {
                // Load current delmål data for the checkboxes
                await updateDelmalSelector();
            }
            document.getElementById(modalId).style.display = 'block';
        }
        
        async function updateDelmalSelector() {
            const selector = document.getElementById('delmalSelector');
            selector.innerHTML = '<div style="text-align: center; padding: 20px;">Laddar delmål...</div>';
            
            const delmalData = await loadDelmalData();
            
            if (delmalData && delmalData.length > 0) {
                // Group by category
                const grouped = delmalData.reduce((groups, delmal) => {
                    const category = delmal.category;
                    if (!groups[category]) {
                        groups[category] = [];
                    }
                    groups[category].push(delmal);
                    return groups;
                }, {});
                
                const categoryNames = {
                    'A': 'A-serien: Allmänna kompetenser',
                    'B': 'B-serien: Patientvård grundläggande', 
                    'C': 'C-serien: Psykiatrispecifika kompetenser'
                };
                
                selector.innerHTML = '';
                
                Object.keys(grouped).sort().forEach(category => {
                    // Add category header
                    const header = document.createElement('div');
                    header.style.cssText = 'font-weight: 600; color: #667eea; margin: 15px 0 8px 0; border-bottom: 1px solid #e2e8f0; padding-bottom: 4px;';
                    header.textContent = categoryNames[category] || category;
                    if (category === 'A') header.style.marginTop = '10px';
                    selector.appendChild(header);
                    
                    // Add checkboxes for this category
                    grouped[category].forEach(delmal => {
                        const checkboxDiv = document.createElement('div');
                        checkboxDiv.className = 'delmal-checkbox';
                        
                        const checkbox = document.createElement('input');
                        checkbox.type = 'checkbox';
                        checkbox.id = `delmal_${delmal.code.toLowerCase()}`;
                        checkbox.value = delmal.code;
                        
                        const label = document.createElement('label');
                        label.htmlFor = checkbox.id;
                        label.textContent = `${delmal.code} - ${delmal.title}`;
                        
                        checkboxDiv.appendChild(checkbox);
                        checkboxDiv.appendChild(label);
                        selector.appendChild(checkboxDiv);
                    });
                });
            } else {
                selector.innerHTML = '<div style="color: #e53e3e; text-align: center; padding: 20px;">Kunde inte ladda delmål. Kontrollera din anslutning.</div>';
            }
        }

        function closeModal(modalId) {
            document.getElementById(modalId).style.display = 'none';
        }

        // Close modal when clicking outside
        window.onclick = function(event) {
            if (event.target.classList.contains('modal')) {
                event.target.style.display = 'none';
            }
        }

        // Add activity function - now saves to Google Sheets!
        async function addActivity() {
            const title = document.getElementById('activityTitle').value;
            const type = document.getElementById('activityType').value;
            const date = document.getElementById('activityDate').value;
            const assessor = document.getElementById('activityAssessor').value;
            const notes = document.getElementById('activityNotes').value;
            
            // Validation
            if (!title || !type || !date || !assessor) {
                showError('Vänligen fyll i alla obligatoriska fält (titel, typ, datum, bedömare)');
                return;
            }
            
            // Get selected delmål
            const selectedDelmal = [];
            document.querySelectorAll('#delmalSelector input[type="checkbox"]:checked').forEach(checkbox => {
                selectedDelmal.push(checkbox.value);
            });
            
            if (selectedDelmal.length === 0) {
                showError('Vänligen välj minst ett delmål för denna aktivitet');
                return;
            }
            
            const activity = {
                title,
                type,
                date,
                assessor,
                delmal: selectedDelmal,
                notes: notes || ''
            };
            
            // Show loading state
            const saveBtn = document.querySelector('#activityModal .add-btn');
            const originalText = saveBtn.textContent;
            saveBtn.textContent = 'Sparar...';
            saveBtn.disabled = true;
            
            // Save to Google Sheets
            const result = await postToAPI('addAktivitet', activity);
            
            // Reset button state
            saveBtn.textContent = originalText;
            saveBtn.disabled = false;
            
            if (result && result.success) {
                showSuccess('Aktivitet sparad till Google Sheets!');
                closeModal('activityModal');
                
                // Reset form
                document.getElementById('activityTitle').value = '';
                document.getElementById('activityNotes').value = '';
                document.getElementById('activityDate').value = '';
                document.getElementById('activityAssessor').value = '';
                document.querySelectorAll('#delmalSelector input[type="checkbox"]').forEach(cb => cb.checked = false);
                
                // Refresh current view
                const activeTab = document.querySelector('.tab.active').onclick.toString().match(/switchTab\('(.+?)'\)/)[1];
                if (activeTab === 'activities') {
                    await loadActivitiesTab();
                } else if (activeTab === 'overview') {
                    await loadRecentActivities();
                    await loadOverviewSidebar();
                }
            } else {
                showError('Fel vid sparande. Försök igen.');
            }
        }

        // Initialize on page load with real data
        document.addEventListener('DOMContentLoaded', async function() {
            console.log('ST Portfolio loaded! Connecting to Google Sheets...');
            
            // Update sync status
            const syncStatus = document.querySelector('.sync-status');
            syncStatus.innerHTML = '🔄 Ansluter till Google Sheets...';
            
            try {
                // Load initial data
                await Promise.all([
                    loadRecentActivities(),
                    loadOverviewSidebar()
                ]);
                
                // Update sync status
                const now = new Date();
                const timeString = now.toLocaleTimeString('sv-SE', { hour: '2-digit', minute: '2-digit' });
                syncStatus.innerHTML = `✅ Synkroniserad med Google Sheets • Senast uppdaterad: Idag ${timeString}`;
                
            } catch (error) {
                console.error('Initialization error:', error);
                syncStatus.innerHTML = '❌ Fel vid anslutning till Google Sheets - kontrollera din internetanslutning';
            }
        });
    </script>
</body>
</html>
